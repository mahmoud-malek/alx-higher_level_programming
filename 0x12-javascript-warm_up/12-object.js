#!/usr/bin/node

// Declare myObject using const
const myObject = {
  type: 'object',
  value: 12
};

// Update the value property
myObject.value = 89;

// Print the modified object
console.log(myObject);
